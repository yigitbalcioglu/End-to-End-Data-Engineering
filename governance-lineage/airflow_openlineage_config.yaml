# Place under AIRFLOW_HOME and set OPENLINEAGE_URL/OPENLINEAGE_API_KEY env vars.
transport:
  type: http
  url: ${OPENLINEAGE_URL:-http://localhost:8585/api/v1/lineage}
  auth:
    type: api_key
    api_key: ${OPENLINEAGE_API_KEY:-replace-me}
